# Credentials — never commit
.env
.env.local

# Playwright MCP browser logs (regenerated each session)
.playwright-mcp/

# Corpus images — excluded from git, re-downloadable from YEK portal via visual-confirmation agent
corpus/yek_screenshots/
corpus/visual_confirmation_*/

# OS/editor artifacts
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo

# Claude Code local settings (machine-specific MCP permissions)
.claude/settings.local.json

# Agent working memory — operational state file updated every session
# Research output is captured in catalog/ and .claude/skills/ instead
MEMORY.md

# Logs and temporary files
logs/
catalog/temp/
catalog/searches/
temp/
temp_snapshot*.md
memory/*

# Root-level working images (re-downloadable from YEK portal)
# Note: examples/ subdirectories are exempt (tracked reference taxonomy)
/*.jpeg
/*.jpg
/*.png

# Playwright browser session snapshots (ephemeral, regenerated each session)
snapshot_*.md
yek_*.md
advsearch_snapshot.md
search[0-9]*.md
search[0-9]*.png

# Node.js
node_modules/
